package com.master.ValuesToBean;

import com.accessibility.action.RoleAction;
import com.login.loginModel.RoleBean;

public class RoleActionToRoleBean {

	public RoleBean setvalues(RoleAction ra) {
		RoleBean rb = new RoleBean();
		
	/*	rb.setNew_user(ra.isNew_user());
		rb.setRole_user(ra.isRole_user());
		
		rb.setChangePassword(ra.isChangePassword());
		rb.setOpennew(ra.isOpennew());
	rb.setControl_user(ra.isControl_user());
	rb.setJobcard_report(ra.isJobcard_report());
	rb.setOpennew_emp_master(ra.isOpennew_emp_master());
	rb.setCustomer_report(ra.isCustomer_report());*/
	
		return rb;
	}
}
